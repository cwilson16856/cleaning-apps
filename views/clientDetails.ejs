<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../partials/_head.ejs %>
    <title>Client Details</title>
</head>
<body>
    <% include ../partials/_header.ejs %>

    <div class="container mt-5">
        <h1>Client Details</h1>
        <!-- Client details will be dynamically inserted here -->
        <div id="clientDetails" class="mt-3">
            <!-- Dynamic client details -->
            <p>Name: <span id="clientName"><%= client.name %></span></p>
            <p>Email: <span id="clientEmail"><%= client.email %></span></p>
            <p>Phone Number: <span id="clientPhoneNumber"><%= client.phoneNumber %></span></p>
            <p>Client ID: <span id="clientId"><%= client._id %></span></p>
        </div>

        <a href="/clients/edit/<%= client._id %>" class="btn btn-primary mt-3">Edit Client</a>
        <a href="/quotes/new/<%= client._id %>" class="btn btn-success mt-3">Create Quote for Client</a>
    </div>

    <% include ../partials/_footer.ejs %>
    <script src="/js/main.js"></script>
</body>
</html>